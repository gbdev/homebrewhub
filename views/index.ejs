<!doctype html>
<html>
  <head>
    <title>Showcase</title>
    <% include head.html %>
  </head>
  <body>
    <% include header.ejs %>
    <div class="container col-lg-9">
      <br>
      <div class="container">
        <div class="card-deck">
        <% games.forEach( function(element){ %>
        <div class="card col-lg-3">
                      <img class="card-img-top" src=/<%= element.data.screenshots[0].data.fslocation %> alt="Card image cap">

          <div class="card-block">
            <a href=<%= "game/"+element.data.title %>><h3 class="card-title"> <%= element.data.title %> </h3></a>
                <p class="card-text">
                  <small class="text-muted"></small>
                  <% element.data.tags.forEach( function(tag) { %>
                  <span class="badge badge-default"><%= tag %></span>
                  <% }) %>
                </p>

          </div>
        </div>

        
        <% }) %>
        </div>
      </div>
      </div>
    </div>
    <% include footer.html %> 

  </body>
</html>